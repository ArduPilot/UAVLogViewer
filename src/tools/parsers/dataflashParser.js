export class MavlinkParser {
  constructor () {
  }

  onMessage (message) {
    // self.postMessage({messages: instance.messages})
  }

  static fixData (message) {
    return message
  }

  processData (data) {
  }
}
